build/
build32/
build-*/
*.pass1-broken-backup
.DS_Store
.vscode/
*.swp
*.bak
*~
._*

*.wav
*.csv
lens_run
node_modules/

# the dumping ground: anything here stays local and never ships
attic/
lens_sim_events.csv
lens_raw.txt

# compiled host sim binary (dev only)
runtime/test/host_runner

# copyrighted DX7 voice data -- never commit (source .syx and generated header)
banks/
runtime/dx7banks.h

# Claude Code local settings, worktrees, and scratch
.claude/
scratchpad/
patches/*.loupe.bin
patches/*.loupe.json
