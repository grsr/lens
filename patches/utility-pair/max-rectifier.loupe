; UTILITY: max / rectifier — the "analogue OR" of two signals (patch b = -a for a full-wave
; rectifier); the gate is high while a is positive.
(def max-rectifier (fn (:a :b => :out :gate)
  (<- out  (max a b))
  (<- gate (gt a 0))))
