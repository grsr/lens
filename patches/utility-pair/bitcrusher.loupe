; UTILITY: bitcrusher — sample-rate reduction (crush) then bit-depth reduction (mask top bits).
; mask is bitwise, so quantise in the unipolar domain to keep the audio's sign.
(def bitcrusher (fn (:in :rate :depth 3584) (bipolar (mask (unipolar (crush in rate)) depth))))
